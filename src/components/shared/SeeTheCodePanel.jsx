// SeeTheCodePanel.jsx — Expandable panel that reveals the generated HTML/JS code.
// Full implementation in see-the-code.feature.md — this is a placeholder stub.

export default function SeeTheCodePanel({ code }) {
  return (
    <div className="my-4 p-4 bg-gray-100 rounded-xl text-sm text-gray-500">
      See the Code panel coming soon
    </div>
  );
}
